# Where is the application at?

It's hosted at https://devopswithdocker-heroku.samip.dev/, yes It's still Heroku.
You can verify by doing `dig devopswithdocker-heroku.samip.dev` that it is in fact hosted there.
